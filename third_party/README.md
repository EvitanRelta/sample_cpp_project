# Third-party

Contains code that originated elsewhere (e.g. libraries).

## Header-only libraries

Header-only libraries go directly into `include/` dir.

- `CLI11` v2.6.2 - Command line parser for C++11 \
  https://github.com/CLIUtils/CLI11
